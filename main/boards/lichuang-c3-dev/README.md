# Lichuang ESP32-C3 Dev

Dokumentasi papan pengembangan ini tersedia di:

- https://wiki.lckfb.com/zh-hans/szpi-esp32c3

Catatan penting:

- Ukuran flash papan ini adalah 8 MB.
- Saat kompilasi, gunakan tabel partisi yang sesuai untuk 8 MB.

Pengaturan partisi yang disarankan:

```text
Partition Table  --->
  Partition Table (Custom partition table CSV)  --->
  (partitions/v2/8m.csv) Custom partition CSV file
```
